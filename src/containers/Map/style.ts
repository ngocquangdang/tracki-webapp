import styled from 'styled-components';

const MapContainer = styled.div`
  width: 100%;
  height: 100%;
`;
const MapBox = styled.div`
  position: absolute;
  top: 0;
  bottom: 0;
  width: 100%;
`;

export { MapBox, MapContainer };
